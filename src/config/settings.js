export const CONFIG = {
    // Carpeta base donde se encuentran los paquetes SCORM
    SCORM_BASE_FOLDER: '/scorm',
    
    // Nombres de los SCORMs de introducción y salida
    INTRO_SCORM_FOLDER: 'intro',
    EXIT_SCORM_FOLDER: 'closing',
    
    // Endpoint de la API para guardar resultados
    API_ENDPOINT: '/api/scorm-results'
  };